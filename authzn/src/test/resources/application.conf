authzn {
  policy {
    directives = [allow-all]
    directive {
      allow-all {
        users = ["*"]
        roles = ["*"]
        resource = /
        actions = ["*"]
        decision = allow
      }
    }
  }
}