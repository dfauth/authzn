authzn {
  policy {
    directives = [user-admin]
    directive {
      user-admin {
        users = []
        roles = ["admin"]
        resource = /users
        actions = ["*"]
        decision = allow
      }
    }
  }
}